#include <bits/stdc++.h>
using namespace std;

class Queue{
    public: 
    int arr[100];
    int rear;
    int front;
    int size;
    
    Queue(){
        rear = -1;
        front = 0;
        size = 0;
        
    }
        
    bool isFull(){
        if(size == 100){
            return true;
        }
        else {
            return false;
            //return size == 100;
        }
    }
            
    bool isEmpty(){
        if(size == 0){
            return true;
        }
        else {
            return false;
            //return size == 0;
        }
        }
            
    void enqueue(int x){
        if(isFull() == true){
            cout << "Queue is Full" << endl;
        }
        else{
            rear = (rear+1)%100;
            arr[rear] = x;
            size++;
        }
    }
    
    void dequeue(){
        if(isEmpty() == true){
            cout << "Queue is Empty" << endl;
        }
        else {
            // front++;
            front = (front+1)%100;
            size--;
        }
    }
    int getFront(){
        if(isEmpty()){
            return -1;
        }
        return arr[front];
    }
        
    int getRear(){
        if(isEmpty()){
            return -1;
        }
        return arr[rear];
    }
        
    void traversal(){
        int temp = front;
        int count = size;
        while (count){
            cout << arr[temp] << " ";
            temp = (temp+1)%100;
            count--;
        }
        cout << endl;
    }
};
    int main() {
        
        Queue q;
        q.enqueue(10);
        q.enqueue(20);
        q.enqueue(30);
        q.enqueue(40);
        q.enqueue(50);
        
        q.dequeue();
        q.dequeue();
        
        q.traversal();
    
    return 0;
}

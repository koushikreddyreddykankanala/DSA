#include <bits/stdc++.h>
using namespace std;

int main() {
    // vector<int>vec;
    
    vector<int>vec(5);
    
    vec.push_back(20);
    vec.push_back(30);
    vec.push_back(112);
    vec.push_back(-30);
    vec.push_back(212);
    
    // vec.pop_back();
    
    int n = vec.size();
    
    for(int i = 0; i < n; i++){
        cout << vec[i] << " ";
    }
    
    return 0;
}
